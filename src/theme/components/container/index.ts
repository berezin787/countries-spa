import { defineStyleConfig } from '@chakra-ui/react';

export const Container = defineStyleConfig({
  baseStyle: {
    'margin': '0 auto',
    'padding': '0 2rem',
    maxW: '100%',
    w: '7xl',
  },
});
